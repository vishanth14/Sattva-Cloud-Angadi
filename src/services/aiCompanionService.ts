export const aiCompanionService = {
  askRootsCompanion: async (query: string): Promise<string> => {
    return `Roots Companion analysis for: "${query}". (Heritage and care intelligence placeholder)`;
  },
};
